<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.officeAuto.ssm.dao.system.EmployeeAndInfoDao" >

  <select id="findAll" resultType="com.officeAuto.ssm.model.EmployeeAndInfo">
    select employee.*,employeeinfo.*,job.name,dept.name from employee left join employeeinfo on employee.uuid=employeeinfo.id left join job on employee.uuid=job.employee left join dept  on dept.uuid=job.dept
  </select>

  <select id="findByPage" resultType="com.officeAuto.ssm.model.EmployeeAndInfo">
    select employee.*,employeeinfo.*,job.name,dept.name from employee left join employeeinfo on employee.uuid=employeeinfo.id left join job on employee.uuid=job.employee left join dept  on dept.uuid=job.dept  limit ${(currentPage-1)*pageSize},${(currentPage-1)*pageSize-1+pageSize}
  </select>

    <select id="findCount" resultType="java.lang.Integer">
      select count(*) from employee left join employeeinfo on employee.uuid=employeeinfo.id left join job on employee.uuid=job.employee left join dept  on dept.uuid=job.dept
    </select>

    <select id="findById" resultType="com.officeAuto.ssm.model.EmployeeAndInfo">

    </select>


    <delete id="deleteById" parameterType="java.lang.Integer" >

    </delete>


    <delete id="delete">

    </delete>

    <insert id="insert" parameterType="com.officeAuto.ssm.model.EmployeeAndInfo">

    </insert>

    <update id="update" parameterType="com.officeAuto.ssm.model.EmployeeAndInfo">

    </update>

</mapper>